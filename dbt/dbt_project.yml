name: claims_analytics
version: "1.0.0"
config-version: 2
profile: claims_analytics
model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]
vars:
  cortex_model: "{{ env_var('CORTEX_MODEL', 'llama3.1-70b') }}"

models:
  claims_analytics:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: table
